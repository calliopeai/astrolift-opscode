# OpenTelemetry collector with Azure Monitor exporter (traces + metrics).
#
# Module is currently a scaffold; resources land in the follow-up commit
# that wires the User-Assigned Managed Identity + federated credential
# binding for the OTel collector ServiceAccount.
