# Velero with the azure-pv-backup add-on — cluster-wide PV snapshots
# to a Storage Account container.
#
# Module is currently a scaffold; resources land in the follow-up commit
# that creates the storage container + UAMI + federated credential binding.
