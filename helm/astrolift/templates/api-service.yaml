{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "astrolift.fullname" . }}-api
  labels:
    {{- include "astrolift.labels" . | nindent 4 }}
    {{- include "astrolift.selectorLabels" (dict "root" . "component" "api") | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "astrolift.selectorLabels" (dict "root" . "component" "api") | nindent 4 }}
{{- end }}
