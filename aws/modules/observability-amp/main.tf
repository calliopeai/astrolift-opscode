# Amazon Managed Prometheus (AMP) workspace + scrape config + IRSA.
#
# Module is currently a scaffold; resources land in the follow-up commit
# that provisions the AMP workspace and grants the cluster IRSA write access.
