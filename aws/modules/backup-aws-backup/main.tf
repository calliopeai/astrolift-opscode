# AWS Backup vault + plan + selection rules for RDS, EFS, DynamoDB.
#
# Module is currently a scaffold; resources land in the follow-up commit
# that creates the vault (KMS-encrypted), plan, and resource selections.
