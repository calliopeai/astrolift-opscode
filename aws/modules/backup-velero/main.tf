# Velero install for cluster-wide PV snapshots to S3.
#
# Module is currently a scaffold; resources land in the follow-up commit
# that creates the backup S3 bucket, IRSA role, and Velero Helm release.
