# Velero with the gcp-pv-backup plugin — cluster-wide PV snapshots to GCS.
#
# Module is currently a scaffold; resources land in the follow-up commit
# that creates the GCS backup bucket + GSA + Workload Identity binding.
