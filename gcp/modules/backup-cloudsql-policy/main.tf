# Cloud SQL backup policy (automated backups + PITR + scheduled exports to GCS).
#
# Module is currently a scaffold; resources land in the follow-up commit
# that wires the backup_configuration block + scheduled export job.
