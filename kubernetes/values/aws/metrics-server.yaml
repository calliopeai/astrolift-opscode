# metrics-server — AWS
# Provides CPU/memory metrics for HPA autoscaling.

replicas: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

# EKS includes metrics-server as a managed addon.
# Only install this if NOT using the EKS addon.
